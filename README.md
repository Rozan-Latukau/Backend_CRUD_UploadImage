# Backend_CRUD_UploadImage

## Available Scripts

In the project directory, you can run:

### `node index.js or nodemon index.js`

Create CRUD and upload images using React JS, Express JS and Node js

